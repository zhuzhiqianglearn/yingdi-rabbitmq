server:
  port: 8888
spring:
  application:
    name: rabbit-provide
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest
    #消息确认配置项

    #    #确认消息已发送到队列(Queue)
    publisher-returns: true
    #    #确认消息已发送到交换机(Exchange)
    publisher-confirm-type: correlated

